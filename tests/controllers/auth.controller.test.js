const { describe, test } = require("@jest/globals");
// const Auth = require("../../src/controllers/auth.controller.js");


describe("Auth Login (auth.controller.js)", () => {
    test("should return a 401 status from login post route", () => {
        
    });
});